import React from 'react'
import { appFireStore } from '../firebase/config'

const useMasterTool = () => {
  const db = appFireStore

  const searchData = () => {

  }

  return (
    <div>useMasterTool</div>
  )
}

export default useMasterTool